{
  "name": "stitch",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index",
  "scripts": {
    "build": "pm install typescript && npm ci && npx tsc && cd client && npm ci && npm run build",
    "start": "node build",
    "dev": "concurrently \"npx tsc --watch\" \"nodemon -q build/index.js\"",
    "preview": "npx tsc && cd client && npm run build && cd .. && node build"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@socket.io/admin-ui": "^0.4.0",
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "socket.io": "^4.5.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^18.0.0",
    "concurrently": "^7.2.2",
    "nodemon": "^2.0.18",
    "typescript": "^4.7.4"
  }
}
